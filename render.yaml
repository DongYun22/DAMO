services:
  - type: web
    name: damo-preview
    runtime: node
    plan: free
    region: singapore
    buildCommand: corepack enable && pnpm install --frozen-lockfile --prod=false && pnpm build:render
    startCommand: pnpm db:migrate && pnpm db:seed && pnpm start
    healthCheckPath: /health
    autoDeployTrigger: commit
    renderSubdomainPolicy: enabled
    envVars:
      - key: DAMO_HOST
        value: 0.0.0.0
      - key: DAMO_SERVE_WEB
        value: "true"
      - key: WEB_BASE_URL
        value: https://damo-preview.onrender.com
      - key: VITE_NAVER_MAP_CLIENT_ID
        sync: false
      - key: NAVER_SEARCH_CLIENT_ID
        sync: false
      - key: NAVER_SEARCH_CLIENT_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false
